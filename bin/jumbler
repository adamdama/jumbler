#!/usr/bin/env ruby

require 'jumbler'

# create the jumbler
Jumbler.new ARGV

=begin
def help
print "
Usage: #{__FILE__} watch --in_folder --out_folder
"
end

if ARGV.empty?
help
exit
else
j = Jumbler.new ARGV
end
=end